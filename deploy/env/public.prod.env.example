APP_ENV=prod
LOG_LEVEL=INFO

# Public API runtime DB user: SELECT-only role, NEVER the webhook writer role.
# See db/grants/prod_rob_public.sql.
DATABASE_URL=postgresql://prod_rob_public:<password>@<db-host>:25060/rob_prod?sslmode=require

# Public read-only API server. Do not add DISCORD_TOKEN on this host.
PUBLIC_API_HOST=127.0.0.1
PUBLIC_API_PORT=8090

# Browser origins allowed to call the API (comma-separated). Entries may be
# exact origins, wildcard patterns (https://*.lovableproject.com), or * to
# allow any origin while iterating. A matching request Origin is reflected back.
# Prod example (site + Lovable preview):
#   PUBLIC_API_ALLOWED_ORIGIN=https://robthebot.com,https://*.lovableproject.com
PUBLIC_API_ALLOWED_ORIGIN=https://robthebot.com
